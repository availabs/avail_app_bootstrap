// This should be moved into a central config file.

const HOST = '<host server address>'
const PORT = '<port of server or empty string>'
const PATH = '<path to the graphql schema static file>'

module.exports = `${HOST}${PORT}${PATH}`;
